// Rule engine is now simplified - the monitor handles LLM calls directly.
// This file is kept for future expansion (e.g., keyword-based routing).

export function initRuleEngine(): void {
  console.log('[RuleEngine] Initialized (vision-based mode)')
}
